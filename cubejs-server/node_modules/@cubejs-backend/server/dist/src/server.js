"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CubejsServer = void 0;
const dotenv_1 = __importDefault(require("@cubejs-backend/dotenv"));
const server_core_1 = __importDefault(require("@cubejs-backend/server-core"));
const shared_1 = require("@cubejs-backend/shared");
const express_1 = __importDefault(require("express"));
const http_1 = __importDefault(require("http"));
const util_1 = __importDefault(require("util"));
const body_parser_1 = __importDefault(require("body-parser"));
const cors_1 = __importDefault(require("cors"));
const websocket_server_1 = require("./websocket-server");
const gracefull_http_1 = require("./server/gracefull-http");
const graceful_middleware_1 = require("./graceful-middleware");
const server_status_1 = require("./server-status");
const { version } = require('../../package.json');
dotenv_1.default.config({
    multiline: 'line-breaks',
});
class CubejsServer {
    core;
    config;
    server = null;
    socketServer = null;
    sqlServer = null;
    status = new server_status_1.ServerStatusHandler();
    constructor(config = {}, systemOptions) {
        this.config = {
            ...config,
            webSockets: config.webSockets || (0, shared_1.getEnv)('webSockets'),
            sqlPort: config.sqlPort || (0, shared_1.getEnv)('sqlPort'),
            pgSqlPort: config.pgSqlPort || (0, shared_1.getEnv)('pgSqlPort'),
            gatewayPort: config.gatewayPort || (0, shared_1.getEnv)('nativeApiGatewayPort'),
            serverHeadersTimeout: config.serverHeadersTimeout ?? (0, shared_1.getEnv)('serverHeadersTimeout'),
            serverKeepAliveTimeout: config.serverKeepAliveTimeout ?? (0, shared_1.getEnv)('serverKeepAliveTimeout'),
            http: {
                ...config.http,
                cors: {
                    allowedHeaders: 'authorization,content-type,x-request-id',
                    ...config.http?.cors,
                },
            },
        };
        this.core = this.createCoreInstance(this.config, systemOptions);
        this.server = null;
    }
    createCoreInstance(config, systemOptions) {
        return new server_core_1.default(config, systemOptions);
    }
    async listen(options = {}) {
        try {
            if (this.server) {
                throw new Error('CubeServer is already listening');
            }
            const app = (0, express_1.default)();
            app.use((0, cors_1.default)(this.config.http.cors));
            app.use(body_parser_1.default.json({ limit: '50mb' }));
            if (this.config.gracefulShutdown) {
                app.use((0, graceful_middleware_1.gracefulMiddleware)(this.status, this.config.gracefulShutdown));
            }
            await this.core.initApp(app);
            const enableTls = (0, shared_1.getEnv)('tls');
            if (enableTls) {
                throw new Error('CUBEJS_ENABLE_TLS has been deprecated and removed.');
            }
            this.server = (0, gracefull_http_1.gracefulHttp)(http_1.default.createServer(options, app));
            if (this.config.webSockets) {
                this.socketServer = new websocket_server_1.WebSocketServer(this.core, this.config);
                this.socketServer.initServer(this.server);
            }
            if (this.config.sqlPort || this.config.pgSqlPort) {
                this.sqlServer = this.core.initSQLServer();
                await this.sqlServer.init(this.config);
            }
            if (this.config.serverKeepAliveTimeout) {
                this.server.keepAliveTimeout = this.config.serverKeepAliveTimeout;
            }
            if (this.config.serverHeadersTimeout) {
                this.server.headersTimeout = this.config.serverHeadersTimeout;
            }
            const PORT = (0, shared_1.getEnv)('port');
            await this.server.listen(PORT);
            return {
                app,
                port: PORT,
                server: this.server,
                version
            };
        }
        catch (e) {
            if (this.core.event) {
                await this.core.event('Dev Server Fatal Error', {
                    error: (e.stack || e.message || e).toString()
                });
            }
            throw e;
        }
    }
    testConnections() {
        return this.core.testConnections();
    }
    // @internal
    handleScheduledRefreshInterval(options) {
        return this.core.handleScheduledRefreshInterval(options);
    }
    // @internal
    runScheduledRefresh(context, queryingOptions) {
        return this.core.runScheduledRefresh(context, queryingOptions);
    }
    // @internal
    async getDriver(ctx) {
        return this.core.getDriver(ctx);
    }
    async close() {
        try {
            if (this.socketServer) {
                await this.socketServer.close();
            }
            if (this.sqlServer) {
                await this.sqlServer.close();
            }
            if (!this.server) {
                throw new Error('CubeServer is not started.');
            }
            await util_1.default.promisify(this.server.close)();
            this.server = null;
            await this.core.releaseConnections();
        }
        catch (e) {
            if (this.core.event) {
                await this.core.event('Dev Server Fatal Error', {
                    error: (e.stack || e.message || e).toString()
                });
            }
            throw e;
        }
    }
    /**
     * Create driver instance.
     *
     * TODO (buntarb): there is no usage of this method across the project.
     */
    static createDriver(dbType, opt) {
        return server_core_1.default.createDriver(dbType, opt);
    }
    static driverDependencies(dbType) {
        return server_core_1.default.driverDependencies(dbType);
    }
    static apiSecret() {
        return process.env.CUBEJS_API_SECRET;
    }
    static version() {
        return version;
    }
    async shutdown(signal, graceful = true) {
        try {
            const timeoutKiller = (0, shared_1.withTimeout)(() => {
                this.core.logger('Graceful Shutdown Timeout Kill', {
                    error: 'Unable to stop Cube.js in expected time, force kill',
                });
                process.exit(1);
            }, 
            // this.server.stop can be closed in this.config.gracefulShutdown, let's add 1s before kill
            ((this.config.gracefulShutdown || 2) + 1) * 1000);
            this.status.shutdown();
            const locks = [
                this.core.beforeShutdown()
            ];
            if (this.socketServer) {
                locks.push(this.socketServer.close());
            }
            if (this.sqlServer) {
                locks.push(this.sqlServer.shutdown(graceful && (signal === 'SIGTERM') ? 'semifast' : 'fast'));
            }
            if (this.server) {
                locks.push(this.server.stop((this.config.gracefulShutdown || 2) * 1000));
            }
            const shutdownAll = async () => {
                try {
                    if (graceful) {
                        // Await before all connections/refresh scheduler will end jobs
                        await Promise.all(locks);
                    }
                    await this.core.shutdown();
                }
                finally {
                    timeoutKiller.cancel();
                }
            };
            await Promise.any([shutdownAll(), timeoutKiller]);
            return 0;
        }
        catch (e) {
            console.error('Fatal error during server shutting down: ');
            console.error(e.stack || e);
            return 1;
        }
    }
}
exports.CubejsServer = CubejsServer;
//# sourceMappingURL=server.js.map