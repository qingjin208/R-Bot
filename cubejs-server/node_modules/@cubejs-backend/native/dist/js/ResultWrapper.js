"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResultArrayWrapper = exports.ResultMultiWrapper = exports.ResultWrapper = void 0;
const index_1 = require("./index");
class BaseWrapper {
    isWrapper = true;
}
class ResultWrapper extends BaseWrapper {
    proxy;
    cache;
    cached = false;
    isNative = false;
    nativeReference;
    jsResult = null;
    transformData;
    rootResultObject = {};
    constructor(input) {
        super();
        if (input.isWrapper) {
            return input;
        }
        if (Array.isArray(input)) {
            this.jsResult = input;
        }
        else {
            this.isNative = true;
            this.nativeReference = input;
        }
        this.proxy = new Proxy(this, {
            get: (target, prop) => {
                // To support iterative access
                if (prop === Symbol.iterator) {
                    const array = this.getArray();
                    const l = array.length;
                    return function* yieldArrayItem() {
                        for (let i = 0; i < l; i++) {
                            yield array[i];
                        }
                    };
                }
                // intercept indexes
                if (typeof prop === 'string' && !Number.isNaN(Number(prop))) {
                    const array = this.getArray();
                    return array[Number(prop)];
                }
                // intercept isNative
                if (prop === 'isNative') {
                    return this.isNative;
                }
                // intercept array props and methods
                if (typeof prop === 'string' && prop in Array.prototype) {
                    const arrayMethod = Array.prototype[prop];
                    if (typeof arrayMethod === 'function') {
                        return (...args) => this.invokeArrayMethod(prop, ...args);
                    }
                    return this.getArray()[prop];
                }
                // intercept JSON.stringify or toJSON()
                if (prop === 'toJSON') {
                    return () => this.getArray();
                }
                return target[prop];
            },
            // intercept array length
            getOwnPropertyDescriptor: (target, prop) => {
                if (prop === 'length') {
                    const array = this.getArray();
                    return {
                        configurable: true,
                        enumerable: true,
                        value: array.length,
                        writable: false
                    };
                }
                return Object.getOwnPropertyDescriptor(target, prop);
            },
            ownKeys: (target) => {
                const array = this.getArray();
                return [...Object.keys(target), ...Object.keys(array), 'length', 'isNative'];
            }
        });
        Object.setPrototypeOf(this.proxy, ResultWrapper.prototype);
        return this.proxy;
    }
    getArray() {
        if (!this.cache) {
            if (this.isNative) {
                this.cache = (0, index_1.getCubestoreResult)(this.nativeReference);
            }
            else {
                this.cache = this.jsResult;
            }
            this.cached = true;
        }
        return this.cache;
    }
    invokeArrayMethod(method, ...args) {
        const array = this.getArray();
        return array[method](...args);
    }
    getRawData() {
        if (this.isNative) {
            return [this.nativeReference];
        }
        return [this.jsResult];
    }
    setTransformData(td) {
        this.transformData = td;
    }
    getTransformData() {
        return [this.transformData];
    }
    setRootResultObject(obj) {
        this.rootResultObject = obj;
    }
    getRootResultObject() {
        return [this.rootResultObject];
    }
    async getFinalResult() {
        return (0, index_1.getFinalQueryResult)(this.transformData, this.getRawData()[0], this.rootResultObject);
    }
    getResults() {
        return [this];
    }
}
exports.ResultWrapper = ResultWrapper;
class BaseWrapperArray extends BaseWrapper {
    results;
    constructor(results) {
        super();
        this.results = results;
    }
    getInternalDataArrays() {
        const [transformDataJson, rawData, resultDataJson] = this.results.reduce(([transformList, rawList, resultList], r) => {
            transformList.push(r.getTransformData()[0]);
            rawList.push(r.getRawData()[0]);
            resultList.push(r.getRootResultObject()[0]);
            return [transformList, rawList, resultList];
        }, [[], [], []]);
        return [transformDataJson, rawData, resultDataJson];
    }
    // Is invoked from the native side to get
    // an array of all raw wrapped results
    getResults() {
        return this.results;
    }
    getTransformData() {
        return this.results.map(r => r.getTransformData()[0]);
    }
    getRawData() {
        return this.results.map(r => r.getRawData()[0]);
    }
    getRootResultObject() {
        return this.results.map(r => r.getRootResultObject()[0]);
    }
}
class ResultMultiWrapper extends BaseWrapperArray {
    rootResultObject;
    constructor(results, rootResultObject) {
        super(results);
        this.rootResultObject = rootResultObject;
    }
    async getFinalResult() {
        const [transformDataJson, rawDataRef, cleanResultList] = this.getInternalDataArrays();
        const responseDataObj = {
            queryType: this.rootResultObject.queryType,
            results: cleanResultList,
            slowQuery: this.rootResultObject.slowQuery,
        };
        return (0, index_1.getFinalQueryResultMulti)(transformDataJson, rawDataRef, responseDataObj);
    }
}
exports.ResultMultiWrapper = ResultMultiWrapper;
// This is consumed by native side via Transport Bridge
class ResultArrayWrapper extends BaseWrapperArray {
    constructor(results) {
        super(results);
    }
    async getFinalResult() {
        const [transformDataJson, rawDataRef, cleanResultList] = this.getInternalDataArrays();
        return [transformDataJson, rawDataRef, cleanResultList];
    }
}
exports.ResultArrayWrapper = ResultArrayWrapper;
//# sourceMappingURL=ResultWrapper.js.map