import type { ZodError } from 'zod';
import { ContextAcceptorFn } from '../interfaces';
import { WsMessage } from './message-schema';
import type { ApiGateway } from '../gateway';
import type { LocalSubscriptionStore } from './local-subscription-store';
export type WebSocketSendMessageFn = (connectionId: string, message: any) => Promise<void>;
export declare class SubscriptionServer {
    protected readonly apiGateway: ApiGateway;
    protected readonly sendMessage: WebSocketSendMessageFn;
    protected readonly subscriptionStore: LocalSubscriptionStore;
    protected readonly contextAcceptor: ContextAcceptorFn;
    constructor(apiGateway: ApiGateway, sendMessage: WebSocketSendMessageFn, subscriptionStore: LocalSubscriptionStore, contextAcceptor: ContextAcceptorFn);
    protected resultFn(connectionId: string, messageId: string | undefined, requestId: string | undefined, logNetworkUsage?: boolean): (message: any, { status }?: {
        status: number;
    }) => Promise<void>;
    protected deserializeMessage(message: any): any;
    protected mapZodError(error: ZodError): string;
    protected validateMessage(message: object): WsMessage;
    processMessage(connectionId: string, body: string): Promise<void>;
    protected handleMessage(connectionId: string, message: WsMessage, isSubscription: boolean): Promise<void>;
    processSubscriptions(): Promise<void>;
    disconnect(connectionId: string): Promise<void>;
    clear(): void;
}
//# sourceMappingURL=subscription-server.d.ts.map