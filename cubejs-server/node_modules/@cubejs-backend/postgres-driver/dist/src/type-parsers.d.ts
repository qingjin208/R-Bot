/** OID 1082 — Postgres emits `YYYY-MM-DD`. */
export declare const dateTypeParser: (val: string) => string;
/** OID 1114 — `YYYY-MM-DD HH:mm:ss` or `YYYY-MM-DD HH:mm:ss.f{1,6}`, no TZ. */
export declare const timestampTypeParser: (val: string) => string;
/**
 * OID 1184 — same as TIMESTAMP, suffixed with `(+|-)HH`, `(+|-)HH:MM`, or
 * `(+|-)HH:MM:SS`. We shift the value into UTC before formatting.
 */
export declare const timestampTzTypeParser: (val: string) => string;
//# sourceMappingURL=type-parsers.d.ts.map